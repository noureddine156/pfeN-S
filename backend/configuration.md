npm install express cors dotenv jsonwebtoken bcryptjs pg :utilisé cette commande pour :==>express → serveur API

cors → communication frontend

dotenv → variables environnement

jsonwebtoken → authentification JWT

bcryptjs → hash password

pg → connexion PostgreSQL

nodemon → auto restart serveur
